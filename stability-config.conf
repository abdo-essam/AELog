// Compose Stability Configuration for AELog
// Classes listed here are treated as Stable by the Compose compiler.
//
// We list only specific classes where the compiler cannot infer stability
// due to external/interface types. Internal data classes with known-stable
// fields are annotated directly with @Immutable or @Stable in source.
//
// See: https://developer.android.com/develop/ui/compose/performance/stability/fix

// Filter classes are open classes with a lambda — compiler can't infer stability
com.ae.log.plugins.log.model.LogSeverityFilter
com.ae.log.plugins.network.model.NetworkFilter
com.ae.log.plugins.analytics.model.AnalyticsFilter
